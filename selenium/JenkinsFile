pipeline {
    agent label 'dotnet'
    stages {
        stage('test') {
            steps {
                bash 'selenium-side-runner  -c "chromeOptions.args=[headless]" project.side'
            }
        }
    }
}