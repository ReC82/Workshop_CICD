pipeline {
    agent { label 'selenium' }
    stages {
        stage('test') {
            steps {
                shell 'selenium-side-runner -c "goog:chromeOptions.args=[disable-infobars, headless] BasicTest.side'
            }
        }
    }
}